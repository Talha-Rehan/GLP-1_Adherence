/**
 * Presentation flags — flip these before a client demo.
 *
 * DEMO_POST_GENERIC_MODE:
 *   true  → payer scenario is locked to "post_generic" (2032+ biosimilar pricing),
 *            the pricing toggle is hidden from the UI.
 *   false → normal behaviour, all three pricing scenarios are selectable.
 */
export const DEMO_POST_GENERIC_MODE = true;
